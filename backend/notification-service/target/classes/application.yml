server:
  port: 8083

spring:
  application:
    name: notification-service
  data:
    mongodb:
      uri: ${NOTIFICATION_MONGODB_URI:mongodb://localhost:27017/notification_db}
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:kien06112004@gmail.com}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
